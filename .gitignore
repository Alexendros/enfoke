# definiciones/ contiene ficheros .md reales versionados por git (la fuente de
# verdad del catálogo). El repo es autocontenido: nada de symlinks externos.

# Temporales y locales
*.tmp
*.bak
.DS_Store
